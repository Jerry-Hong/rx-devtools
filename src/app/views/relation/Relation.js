import React, { Component } from 'react';

class Relation extends Component {
    render() {
        return (
            <div>
                Not finish feature ~ Working on it
            </div>
        );
    }
}

export default Relation;
